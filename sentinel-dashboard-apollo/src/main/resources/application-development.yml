server:
  port: 8085
  address: 10.104.5.38
  servlet:
    context-path: /
    session:
      timeout: 7200

spring:
  application:
    name: agency
  http:
    encoding:
      force: true
      charset: UTF-8
      enabled: true
#  cloud:
#    sentinel:
#      transport:
#        port: 8719
#        dashboard: ${server.address}:${server.port}

#csp:
#  sentinel:
#    dashboard:
#      server: ${server.address}:${server.port}
#    api:
#      port: 8719
#    heartbeat:
#      interval:
#        ms: 10000

#sentinel:
##    autoRemoveMachineMillis: 30000
#    version: ${project.version}

auth:
  filter:
    exclude-urls: '/,/auth/login,/auth/logout,/registry/machine,/version'
    exclude-url-suffixes: 'htm,html,js,css,map,ico,ttf,woff,png'
  username: sentinel
  password: sentinel

logging:
  file: '${user.home}/logs/csp/sentinel-dashboard.log'
  level:
    org.springframework.web: INFO
  pattern:
    file: '%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n'
    console: '%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n'

apollo:
  cluster: default
  appId: sentinel-dashboard
  portalUrl: http://10.4.99.4:18076
  nameSpace: application
  env: DEV
  token: 6916de41ea47568c519f69f1a9998f7156fcfee6